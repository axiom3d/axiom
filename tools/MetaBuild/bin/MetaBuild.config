<!--
 ! Provides configuration options for MetaBuild.
 !
 ! IMPORTANT NOTE: This file must remain compatible with MSBuild v2
 !   so that it can work in the context of older Visual Studio 2005 projects.
 !-->
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!--
   ! Import add-ins.
   !
   ! To register a new add-in, add an Import directive similar to the
   ! following within this file:
   !
   !   <Import Project="MyAddin.addin" />
   !-->

  <Import Project="MetaBuild.MSBuildCommunityTasks.addin" />

  <Import Project="MetaBuild.CommonProject.addin" />
  <Import Project="MetaBuild.ASPNetWebSite.addin" />
  <Import Project="MetaBuild.DeploymentProject.addin" />
  <Import Project="MetaBuild.Solution.addin" />
  <Import Project="MetaBuild.FileManipulation.addin" />
  <Import Project="MetaBuild.VisualCProject.addin" />
  <Import Project="MetaBuild.VisualBasic6Project.addin" />
  <Import Project="MetaBuild.Module.addin" />
</Project>